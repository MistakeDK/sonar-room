{
  "name": "types",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "projectType": "library",
  "sourceRoot": "libs/types/src",
  "targets": {
    "lint": {
      "executor": "nx:run-commands",
      "options": {
        "command": "tsc -p libs/types/tsconfig.lib.json --noEmit && eslint \"libs/types/src/**/*.ts\""
      }
    },
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "command": "tsc -p libs/types/tsconfig.lib.json --noEmit"
      }
    }
  },
  "tags": [
    "scope:shared",
    "type:domain"
  ]
}

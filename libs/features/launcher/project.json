{
  "name": "feature-launcher",
  "$schema": "../../../node_modules/nx/schemas/project-schema.json",
  "projectType": "library",
  "sourceRoot": "libs/features/launcher/src",
  "targets": {
    "lint": {
      "executor": "nx:run-commands",
      "options": {
        "command": "tsc -p libs/features/launcher/tsconfig.lib.json --noEmit && eslint \"libs/features/launcher/src/**/*.{ts,vue}\""
      }
    },
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "command": "tsc -p libs/features/launcher/tsconfig.lib.json --noEmit"
      }
    }
  },
  "tags": [
    "scope:shared",
    "type:feature"
  ]
}